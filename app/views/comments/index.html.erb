<h1>Post <% @post.title %> + Comments</h1>
<h4>An installment Cooking with Chef Dee</h4>

<p><%@post.body%></p>

<hr>

<h4>Comments</h4>
<div>
    <div class="comment-container" > 
        <% @comments.each do |c| %>
    <p><strong><%= c.title%></strong></p>
            <p><%= c.body%></strong></p>
    </div>
    
    <% end %>
</div>

<%= link_to 'Back', root_path %>